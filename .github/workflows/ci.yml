name: Continuous Integration

on:
  push:
    paths-ignore:
      - 'docs/**'
      - '*.md'
  pull_request:
    paths-ignore:
      - 'docs/**'
      - '*.md'

jobs:
  test:
    uses: climba03003/workflows/.github/workflows/ci-general.yml@v2